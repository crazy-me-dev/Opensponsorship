'use strict';

describe('athleteProfile.athletes module', function() {

  beforeEach(module('athleteProfile.athletes'));

  describe('athletes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var athletesCtrl = $controller('AthletesCtrl');
      expect(athletesCtrl).toBeDefined();
    }));

  });
});